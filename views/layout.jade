!!!
html
  head

    script(src='/client/lib/require.js', type='text/javascript')
    script(src='/client/lib/underscore-min.js', type='text/javascript')
    script(src='/client/lib/mootools-core-1.3.2-full-nocompat.js', type='text/javascript')
    script(src='/client/lib/mootools-more-1.3.2.1.js', type='text/javascript')
    script(src='/client/lib/mooml.js', type='text/javascript')
    script(src='/client/lib/mochaui.js', type='text/javascript')
    script(src='/client/lib/jquery.js', type='text/javascript')

    link(rel='stylesheet', href='/stylesheets/reset.css')
    link(rel='stylesheet', href='/stylesheets/sheet.css')
    link(rel='stylesheet', href='/stylesheets/headers.css')

    script(type='text/javascript')

      require({
        paths: {
            "platform" : "/javascripts/platform",
            "unit"     : "/javascripts/unit"
        },
      });

      MUI.initialize({
        'path': {
            root:  '/client/lib/mochaui/',
            //themes:  '/client/lib/mochaui/Source/Themes/',
            //plugins: '/client/lib/mochaui/Source/Plugins/',
            //controls:'/client/lib/mochaui/Source/Controls/'
        }  
      });

      MUI.load('MUI.Dock');
      MUI.load('MUI.Toolbar');
      MUI.load('MUI.Window');

      jQuery.noConflict();

      var oldAttach = require.attach;
      require.attach = function (url, contextName, moduleName, callback, type) {
          url += (url.indexOf('?') === -1 ? '?' : '&') + 'bust=' + (new
              Date()).getTime()
          return oldAttach.call(require, url, contextName, moduleName,
              callback, type);
      }

      //require(["platform/start"])

  body!= body